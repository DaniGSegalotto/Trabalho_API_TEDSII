// Importa o módulo axios para realizar requisições HTTP
import axios from "axios";

// Define a URL base para a API
const urlBase = "http://localhost:3001/api";

// Cria uma classe UsuarioService para gerenciar operações relacionadas a usuários
class UsuarioService {
  // Aqui você pode adicionar métodos para interagir com a API, como cadastro, autenticação, etc.
}

// Exporta uma nova instância da classe UsuarioService
export default new UsuarioService();
